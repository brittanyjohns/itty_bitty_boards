<% content_for :page_title, "Edit #{@product.name}" %>

<div class="flex items-center justify-between mb-6">
  <h1 class="text-2xl font-semibold text-t1 tracking-tight">Edit <%= @product.name %></h1>
  <%= link_to "← Back to the product", admin_dashboard_printable_product_path(@product), class: "text-xs text-t2 hover:text-t1" %>
</div>

<%= render "form" %>
