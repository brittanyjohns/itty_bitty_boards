<% content_for :page_title, "New printable product" %>

<div class="flex items-center justify-between mb-6">
  <h1 class="text-2xl font-semibold text-t1 tracking-tight">New printable product</h1>
  <%= link_to "← All products", admin_dashboard_printable_products_path, class: "text-xs text-t2 hover:text-t1" %>
</div>

<%= render "form" %>
