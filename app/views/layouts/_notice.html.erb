<% if notice.present? %>
  <div class="flex justify-around items-center border-b p-2">
    <p class="py-2 px-5 bg-green-50 mb-1 text-green-500 font-semibold text-xl rounded-lg inline-block mx-auto" id="notice"><%= notice %></p>
  </div>
<% end %>
