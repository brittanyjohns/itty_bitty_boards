<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Board PDF</title>
    <style>
      html, body {
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
        margin: 0;
        padding: 0;
        height: 100%;
        width: 100%;
      }

      body {
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
      }

      @page {
        margin: 0;
        size: <%= @landscape ? "letter landscape" : "letter" %>;
      }
      /* One-page canvas */
      .page {
        height: 100vh;          /* lock to one page viewport */
        width: 100vw;
        overflow: hidden;       /* prevents spilling to page 2 */

        display: grid;
        grid-template-rows: auto 1fr;  /* header + board */
        box-sizing: border-box;
        padding: 3mm;

        page-break-inside: avoid !important;
        page-break-after: avoid !important;
      }

      .header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin: 0;
        padding: 0;
      }

      .logo {
        width: 14mm;
        height: auto;
        margin-left: 4mm;
      }

      .logo img {
        max-width: 14mm;
        height: auto;
        object-fit: contain;
      }

      .qr-small img {
        width: 0.8in;
        height: 0.8in;
        display: inline-block;
        margin-left: 4mm;
      }

      /* Board area must be shrinkable */
      .board-wrap {
        min-height: 0;    /* CRITICAL */
        height: 100%;
        box-sizing: border-box;
        padding: 2pt;
        padding-bottom: 0;
        break-inside: avoid;
      }

      .board-container {
        height: 100%;
        min-height: 0;    /* CRITICAL */
      }

      .board-grid {
        display: grid;
        gap: 2pt;
        width: 100%;
        height: 100%;
        min-height: 0;    /* CRITICAL */

        /* Forces rows to share the available height instead of growing */
        grid-auto-rows: minmax(0, 1fr);
      }

      .tile {
        min-height: 0;    /* CRITICAL */
        overflow: hidden;

        position: relative;
        border: 2pt solid #ccc;
        border-radius: 4pt;
        background: #fff;

        display: grid;
        grid-template-rows: 1fr auto;
      }

      .tile img {
        width: 100%;
        height: 100%;
        min-height: 0;
        object-fit: contain;
        display: block;
      }

      .label {
        font-size: 9pt;
        text-align: center;
        padding: 1pt 2pt;
        line-height: 1.1;
        background: #f7f7f7;
        // border-top: 1px solid #e5e5e5;

        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }

      /* Color classes */
      .green  { background-color: #b7f399; }
      .yellow { background-color: #fdec00; }
      .red    { background-color: #ff6464; }
      .blue   { background-color: #81d9ff; }
      .purple { background-color: #cb91eb; }
      .orange { background-color: #ffc463; }
      .black  { background-color: #000000d9; color: white; }
      .white  { background-color: #ffffffd9; }
      .gray   { background-color: #808080d9; }
      .pink   { background-color: #ffc0e4; }
      .teal   { background-color: #18ddda; }

      /* If you want label to inherit tile bg when colors enabled */
      // .label.green,
      // .label.yellow,
      // .label.red,
      // .label.blue,
      // .label.purple,
      // .label.orange,
      // .label.black,
      // .label.white,
      // .label.gray,
      // .label.pink,
      // .label.teal {
      //   background-color: inherit;
      //   border-top-color: rgba(0,0,0,0.12);
      // }
    </style>
  </head>
  <body>
    <div class="page">
      <%= yield %>
    </div>
  </body>
</html>
