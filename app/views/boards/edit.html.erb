<div class="py-6 px-4 h-screen">
  <h1 class="text-3xl font-bold text-blue-600 mb-6 text-center">Edit Board</h1>
  <%= link_to "Build board", build_board_path(@board), class: "text-blue-600 hover:text-blue-800 underline" %>
  <%= render "form", board: @board %>
  <div class="mt-6 text-center">
    <%= link_to "Back to Boards", boards_path, class: "text-blue-600 hover:text-blue-800 underline" %>
  </div>
</div>
